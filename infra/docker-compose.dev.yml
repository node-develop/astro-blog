# Apply with: docker compose -f docker-compose.yml -f infra/docker-compose.dev.yml up -d
services:
  caddy:
    profiles: ["disabled"]

  api:
    ports:
      - "3001:3001"

  render:
    ports:
      - "3002:3002"

  agents:
    ports:
      - "8000:8000"

  admin:
    ports:
      - "8080:80"
