import { posts } from "./posts";
import { revisions } from "./revisions";
import { media } from "./media";
import { site } from "./site";

export const server = {
  posts,
  revisions,
  media,
  site,
};
